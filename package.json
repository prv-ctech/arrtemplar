{
  "name": "animehub",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "packageManager": "bun@1.3.14",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "bun run dev:server & bun run dev:web",
    "dev:server": "bun run --cwd apps/server dev",
    "dev:web": "bun run --cwd apps/web dev",
    "build": "bun run --cwd packages/shared build && bun run --cwd packages/anime-parser build && bun run --cwd apps/server build && bun run --cwd apps/web build",
    "typecheck": "bun run --cwd packages/shared typecheck && bun run --cwd packages/anime-parser typecheck && bun run --cwd apps/server typecheck && bun run --cwd apps/web typecheck",
    "test": "bun test",
    "check": "biome check . && bun run typecheck && bun test",
    "format": "biome check --write .",
    "db:generate": "bun run --cwd apps/server db:generate",
    "db:migrate": "bun run --cwd apps/server db:migrate",
    "db:seed": "bun run --cwd apps/server db:seed"
  },
  "devDependencies": {
    "@biomejs/biome": "latest",
    "bun-types": "^1.3.14",
    "typescript": "latest"
  }
}
